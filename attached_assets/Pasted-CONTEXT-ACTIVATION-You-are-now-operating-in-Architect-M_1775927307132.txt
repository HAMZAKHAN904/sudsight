CONTEXT ACTIVATION:
You are now operating in "Architect Mode." You are a principal engineer at a Y-Combinator backed startup. I am the non-technical founder. I require a Production-Ready, Publishable Mobile Application. Do not summarize. Do not use placeholders like "// implement logic here". You will provide the complete, unabridged code for every file requested. The app is called "Subsight".

SECTION 1: ABSOLUTE REQUIREMENTS & ENVIRONMENT CONSTRAINTS
Platform: React Native (Expo Managed Workflow) SDK 51.

Language: Strict TypeScript. Use interface over type where possible. No any types.

Routing: Expo Router v3 (File-based). Tabs layout with (tabs) directory.

Styling:

Library: NativeWind v4 (TailwindCSS for React Native).

Dark Mode: Mandatory. Every component must look pristine in both Light and Dark mode. Use classNames with dark: variants.

Theme Variables: Define a colors.js file that maps Tailwind colors to semantic tokens (e.g., primary: '#6366F1').

Data Fetching: TanStack Query v5 (React Query). All data is live-subscribed via Supabase Realtime. Cache must be invalidated within 100ms of a database change.

Forms: React Hook Form with Zod validation schemas.

SECTION 2: FRONTEND ARCHITECTURE & FILE STRUCTURE
I want you to generate a Code Map. You will provide the full file contents for the following files. Do not skip any.

text
subsight/
├── app/
│   ├── _layout.tsx               # Root Layout (React Query Provider, Theme, Safe Area)
│   ├── index.tsx                 # Redirect logic (auth check)
│   ├── (auth)/
│   │   ├── sign-in.tsx           # Magic Link UI (sleek, animated)
│   │   └── sign-up.tsx
│   ├── (tabs)/
│   │   ├── _layout.tsx           # Tab Bar config (Icons: Home, Chart, Wallet, Profile)
│   │   ├── index.tsx             # DASHBOARD SCREEN (The centerpiece)
│   │   ├── analytics.tsx         # Spending trends, charts, insights
│   │   ├── subscriptions.tsx     # List view with swipe-to-delete and search
│   │   └── profile.tsx           # Settings, Currency, Notifications, Logout
│   ├── subscription/
│   │   ├── [id].tsx              # Detailed view/edit screen for a single sub
│   │   └── add.tsx               # Form screen for new subscription
│   └── +not-found.tsx
├── components/
│   ├── ui/                       # Primitives using NativeWind
│   │   ├── Text.tsx
│   │   ├── Card.tsx
│   │   ├── Button.tsx
│   │   ├── Input.tsx
│   │   └── Select.tsx
│   ├── dashboard/
│   │   ├── BurnCard.tsx          # Animated number ticker for monthly total
│   │   ├── RenewalWidget.tsx     # Horizontal list of upcoming dates
│   │   └── DonutChartWrapper.tsx # Victory Native XL implementation
│   ├── forms/
│   │   └── SubscriptionFormFields.tsx # Reusable fields for Add/Edit
│   └── shared/
│       └── EmptyState.tsx
├── lib/
│   ├── supabase.ts               # Supabase client init (with Realtime config)
│   ├── queryClient.ts            # TanStack Query config
│   └── notifications.ts          # expo-notifications logic & scheduling
├── stores/
│   └── authStore.ts              # Zustand store for user session (persisted)
├── utils/
│   ├── calculations.ts           # CRITICAL: Month-end rounding, pro-rata logic
│   ├── dates.ts                  # Date formatting, renewal logic
│   └── currency.ts               # Formatting with Intl
└── types/
    └── database.types.ts         # Generated Supabase Types (Provide a representative snippet)
SECTION 3: CRITICAL LOGIC REQUIREMENTS (Verbatim Code Required)
3.1 The Core Calculation Algorithm (utils/calculations.ts)
You must write a pure function getMonthlyEquivalent. Edge cases:

Weekly: (cost * 52) / 12

Bi-Weekly: (cost * 26) / 12

Quarterly: cost / 3

Yearly: cost / 12

Custom Days: (cost / cycle_days) * 30.44 (using the exact average days per month)

3.2 The "Next Renewal" Prediction (utils/dates.ts)
Given start_date (Date) and cycle (string), return the next upcoming date. It must NOT return a past date. If the subscription was due yesterday, it must return next month's date.

3.3 The Dashboard Screen Logic (app/(tabs)/index.tsx)
This screen needs to do three things simultaneously with React Query:

Fetch profile and subscriptions.

Subscribe to supabase.channel('subscriptions_changes') to invalidate the query cache instantly.

Display a Skeleton Loader while fetching, then a Smooth Animated Counter for the total monthly burn (using react-native-reanimated).

3.4 The Add Subscription Form (app/subscription/add.tsx)

Field 1: Service Name. As user types, debounce and hit the Clearbit Logo API (https://logo.clearbit.com/domain.com) to auto-fill the logo URL.

Field 2: Billing Cycle Picker. A custom wheel picker (or modern bottom sheet) for selecting Weekly, Monthly, Yearly, or "Custom Days".

Field 3: Category Selector. Grid of icons (Fitness, Movie, Cloud, etc.) with a color picker.

SECTION 4: UI/UX SYSTEM DESIGN (The "Feel" Specification)
You are building this for an audience that loves Apple Health and Linear.app. The app must feel calm and premium.

Background Gradient: The main background should be bg-white dark:bg-gray-950 with a subtle radial gradient in the top right corner (Indigo 500/10).

Haptic Feedback: Use expo-haptics for:

Pull-to-refresh (impactLight).

Toggling a subscription active state (notificationSuccess / notificationWarning).

Completing the add form (notificationSuccess).

Animations:

Use react-native-reanimated for entering animations on list items. They should slide up and fade in with a Stagger effect.

The "Burn Card" number should tick up like an odometer (use react-native-animated-numbers or custom Reanimated loop).

Empty State Illustration: Use @expo/vector-icons and a custom Lottie animation (I will provide the JSON asset URL later, but build the component to accept a LottieView). For now, use a graceful SVG constructed with react-native-svg.

SECTION 5: NOTIFICATIONS SERVICE (lib/notifications.ts)
You must provide a fully asynchronous background function that:

Requests permissions on first app open.

Iterates through all active subscriptions.

Schedules a local notification for 24 hours before the next_renewal_date.

Important: It must cancelAllScheduledNotificationsAsync() and re-schedule them every time the app is opened or a subscription is edited. This ensures reminders are always accurate.

SECTION 6: DEPLOYMENT & CONFIGURATION
Please output a step-by-step checklist for taking this codebase to the Apple App Store.

EAS Build Config: Provide the exact eas.json for iOS Production and Android Production.

Supabase Edge Functions: Provide the code for an Edge Function that sends a Weekly Email Digest (top 3 upcoming bills) via Resend/SendGrid. (The user expects this as a bonus feature for the "super prompt").

SECTION 7: OUTPUT FORMAT CONSTRAINT
CRITICAL INSTRUCTION TO AI:
Do not just describe the app. I need the complete codebase text.
Respond in the following exact structure:

ROOT LAYOUT: app/_layout.tsx (Code Block)

CALCULATION UTILS: utils/calculations.ts (Code Block)

DASHBOARD SCREEN: app/(tabs)/index.tsx (Code Block)

ADD FORM: app/subscription/add.tsx (Code Block)

THEME CONFIG: tailwind.config.js (Code Block)

SUPABASE EDGE FUNCTION: (Code Block)

EAS CONFIG: (Code Block)

Final Verdict Request: After providing the code, provide a brief, executive summary of the Monthly Burn Calculation Accuracy and Offline Capability Strategy.

[END OF SUPER PROMPT – REVISED]